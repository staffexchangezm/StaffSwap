<?php get_header(); if ( staffswap_has_builder_content() ) : while ( have_posts() ) : the_post(); the_content(); endwhile; get_footer(); return; endif; ?><section class="account-page"><div class="account-intro"><p class="eyebrow">WELCOME BACK</p><h1>Sign in to StaffExchangeHub</h1><p>Access your requests, saved matches, and private conversations.</p></div><?php echo do_shortcode( '[staffswap_login]' ); ?></section><?php get_footer(); ?>